//a function that takes three values and returns the largest of the three. in int main() read three values and invoke the function. display the largest value
#include<stdio.h>

//int max,min,N;
int max(int N)
{
    if(N<)
    max=N;
    min=N;
    return (max,min);
}
int main()
{
    int c=0;
    while(c<3)
    {
    printf("Enter an Integer:");
    scanf("%d",&N);
    maxmin(N);

       // printf("Max is %d\nMin is %d\n",max,min);
        c++;
    }
    printf("Max is %d\nMin is %d\n",maxmin(max,min));
    return 0;
}

